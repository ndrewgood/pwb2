{
    "build": {
        "env": {
            "JEKYLL_ENV": "production",
            "RUBY_VERSION": "2.7.4"
        }
    },
    "buildCommand": "bundle install && jekyll build",
    "outputDirectory": "_site",
    "framework": "jekyll"
} 